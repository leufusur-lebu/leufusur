; PHP-FPM lee este archivo por-directorio (funciona en Herd local y en producción).
; Sube los límites de subida para permitir fotos de teléfono y varios archivos.
; No se expone públicamente: nginx bloquea los dotfiles.
upload_max_filesize = 12M
post_max_size = 100M
max_file_uploads = 30
